import React from 'react';
import UserDetails from '../Components/UserDetails';

const Show = () => {
    return (
        <div>
            <UserDetails />
        </div>
    );
};

export default Show;