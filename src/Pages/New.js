import React from 'react';
import MakeProfile from '../Components/MakeProfile';

const New = () => {
    return (
        <div>

            <MakeProfile />
        </div>
    );
};

export default New;